rules:
  - name: "Invalid YAML: missing colon"
    source:
      source_name: "saved"
    match # Missing colon
      matcher_name: "all"
    processor:
      processor_name: "log"
      level: "debug"
